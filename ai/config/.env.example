###############################################
#  🔖  .env — 법률 AI 플랫폼 환경설정 파일
#  이 파일을 .env로 복사한 후 실제 값으로 수정하세요.
#  보안을 위해 .gitignore에 등록되어 있습니다.
###############################################

# ===========================================
# 🤖 LLM 및 AI 모델 설정
# ===========================================

# OpenAI API 키 (GMS 시스템 사용)
# https://platform.openai.com/api-keys 또는 GMS에서 발급받은 키
GMS_KEY=your_openai_api_key_here

# LLM 최대 재시도 횟수 (기본값: 3)
LLM_MAX_RETRIES=3

# 채팅 히스토리 최대 토큰 수 (기본값: 3000)
MAX_HISTORY_TOKENS=3000

# 임베딩 모델 이름 (기본값: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2)
EMBEDDING_MODEL_NAME=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# 교차 인코더 모델 이름 (기본값: cross-encoder/ms-marco-MiniLM-L-6-v2)
CROSS_ENCODER_MODEL_NAME=cross-encoder/ms-marco-MiniLM-L-6-v2

# ===========================================
# 🗄️ 데이터베이스 설정 (PostgreSQL + pgvector)
# ===========================================

# DB 접속 계정 이름
DB_USER=postgres

# DB 계정 비밀번호 — 절대 깃 저장소에 커밋하지 마세요!
DB_PASSWORD=your_db_password_here

# DB 서버 호스트
#   - 로컬 개발: localhost 또는 127.0.0.1
#   - Docker: db 또는 localhost
#   - 원격 서버: your-db-server.com
DB_HOST=localhost

# DB 포트 (PostgreSQL 기본값: 5432)
DB_PORT=5432

# 데이터베이스 이름
DB_NAME=legal_ai_db

# ===========================================
# 🌐 API 서버 설정
# ===========================================

# API 서버 호스트 (기본값: 0.0.0.0)
API_HOST=0.0.0.0

# API 서버 포트 (기본값: 8000)
API_PORT=8000

# 개발 모드 자동 리로드 (기본값: true)
API_RELOAD=true

# CORS 허용 도메인 (쉼표로 구분)
# 예: "http://localhost:3000,http://localhost:5173,https://yourdomain.com"
CORS_ORIGINS=http://localhost:5173,https://i13b204.p.ssafy.io

# ===========================================
# 🔍 검색 엔진 설정
# ===========================================

# 기본 페이지 크기 (기본값: 10)
DEFAULT_PAGE_SIZE=10

# 최대 페이지 크기 (기본값: 50)
MAX_PAGE_SIZE=50

# 벡터 검색 초기 후보 개수 (기본값: 20)
VECTOR_SEARCH_TOP_K=20

# 교차 인코더 재순위화 사용 여부 (기본값: true)
USE_RERANK_DEFAULT=true

# ===========================================
# 📝 로깅 설정
# ===========================================

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 로그 포맷
LOG_FORMAT=[%(asctime)s][%(levelname)s][%(name)s] %(message)s

# 로그 파일 경로 (선택사항, 비어있으면 기본 logs/ 디렉토리 사용)
# LOG_FILE_PATH=/path/to/your/logfile.log

# ===========================================
# 🔧 개발자 옵션 (선택사항)
# ===========================================

# Python 경로 추가 (테스트 시 필요할 수 있음)
# PYTHONPATH=

# 추가 GMS 설정 (SSAFY 전용, 필요한 경우 주석 해제)
GMS_BASE_URL=https://gms.ssafy.io/gmsapi/api.openai.com/v1
MODEL_NAME=gpt-4o-mini